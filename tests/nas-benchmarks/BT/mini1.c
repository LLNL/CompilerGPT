
#include <stdio.h>

int main()
{
    // Combine all messages into a single printf call
    printf("Hello World\
                 SUCCESSFUL\
Time in seconds =  11.23\
");

    // (Optional: Use formatted strings if real-value printing is needed in a larger scope)
    // double time = 11.23;
    // printf("Hello World\
                 SUCCESSFUL\
Time in seconds =  %.2f\
", time);
}
